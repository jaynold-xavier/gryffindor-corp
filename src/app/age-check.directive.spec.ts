import { AgeCheckDirective } from './age-check.directive';

describe('AgeCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeCheckDirective();
    expect(directive).toBeTruthy();
  });
});
